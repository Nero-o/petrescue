name: pet_rescue
description: Um aplicativo para adoção e resgate de animais.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  cached_network_image: ^3.1.0
  carousel_slider: ^4.0.0
  google_fonts: ^2.1.1
  flutter_svg: ^1.0.3
  glassmorphism_ui: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
  assets:
    - assets/icons/
